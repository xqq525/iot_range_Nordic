# CLAUDE.md

减少LLM常见编码错误的行为指南。根据需要与项目特定指令合并使用。

**权衡说明：**这些指南倾向于谨慎而非速度。对于简单任务，请酌情使用。

## 1. 编码前先思考

**不要假设。不要隐藏困惑。暴露权衡。**

在实施之前：
- 使用中文明确陈述你的假设。如果不确定，请询问。
- 思考过程用中文展示。
- 如果存在多种解释，请全部列出 - 不要默默选择。
- 如果存在更简单的方法，请说出来。在必要时提出反对意见。
- 如果有不清楚的地方，请停下来。说明困惑之处。提问。

## 2. 简洁优先

**解决问题的最少代码。不做推测性实现。**

- 不添加请求之外的功能。
- 不为单次使用的代码创建抽象。
- 不添加未被要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景添加错误处理。
- 如果你写了200行代码但50行就能解决，请重写。

问问自己："资深工程师会说这太复杂了吗？"如果是，请简化。

## 3. 外科手术式修改

**只触碰必须修改的部分。只清理你自己造成的混乱。**

编辑现有代码时：
- 不要"改进"相邻的代码、注释或格式。
- 不要重构没有问题的代码。
- 匹配现有风格，即使你会采用不同方式。
- 如果注意到无关的死代码，请提及 - 但不要删除。

当你的修改产生孤立代码时：
- 删除由你的修改导致未使用的导入/变量/函数。
- 除非被要求，否则不要删除原本就存在的死代码。

检验标准：每一行修改都应该直接追溯到用户的请求。

## 4. 目标驱动执行

**定义成功标准。循环直到验证通过。**

将任务转化为可验证的目标：
- "添加验证" → "为无效输入编写测试，然后使其通过"
- "修复bug" → "编写能重现问题的测试，然后使其通过"
- "重构X" → "确保重构前后测试都通过"

对于多步骤任务，陈述简要计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

强成功标准能让你独立循环。弱标准（"让它工作"）需要不断澄清。
编码完成需编译看是否通过。

---
## 5. 协议变更



**这些指南是否有效的标志：**差异中不必要的修改更少，因过度复杂而重写的情况更少，澄清性问题出现在实施之前而非犯错之后。
